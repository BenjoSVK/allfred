import {
    Menu,
    MenuButton,
} from '@chakra-ui/react'

const Filter = () => {
    return (
        <div>
            <Menu>
                <MenuButton
                    // as={IconButton}
                    // aria-label='Options'
                    // // icon={<HamburgerIcon />}
                    // variant='outline'
                    >
                </MenuButton>
            </Menu>
        </div>
    )
}

export default Filter